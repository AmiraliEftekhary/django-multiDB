# django-multiDB
using psql as default database and mongodb as the second database
the core is dbRouter
router is writen is db_routers
for connecting to mongodb first instal djongo using "pip install djongo"
